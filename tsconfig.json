{
    "compilerOptions": {
        "outDir": "./dist", // Specify an output directory
        "rootDir": "./src", // Specify the root directory of your source files
        "allowJs": true,
    },
    "include": ["src/**/*"], // Ensure you are only including source files
    
    
}